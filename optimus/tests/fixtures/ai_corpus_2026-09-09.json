{
 "model": "qwen3-coder:30b",
 "captured": "2026-09-09",
 "session": "redacted",
 "labels": [
  "correct",
  "safe-directional",
  "wrong",
  "harmful"
 ],
 "cases": [
  {
   "name": "3q1dsfrmti",
   "finding_type": "N+1 Query",
   "severity": "High",
   "title": "Same query ran 50× at python/common.py:280",
   "customer_description": "We noticed the same query was repeated 50 times in a row from the same line of code (ugly_code/ugly_code/python/common.py:280), costing about 32ms. This is usually a Python loop that should fetch its data in one query instead of one-at-a-time. Typical fix: a few hours of dev work.",
   "estimated_impact_ms": 31.71,
   "affected_count": 50,
   "action_ref": "12",
   "technical_detail": {
    "callsite": {
     "filename": "ugly_code/ugly_code/python/common.py",
     "lineno": 280,
     "function": "bg_recheck_users",
     "source_snippet": [
      {
       "lineno": 278,
       "content": "        try:"
      },
      {
       "lineno": 279,
       "content": "            user = frappe.get_doc(\"User\", frappe.session.user)"
      },
      {
       "lineno": 280,
       "content": "            frappe.db.sql("
      },
      {
       "lineno": 281,
       "content": "                \"SELECT role FROM `tabHas Role` WHERE parent=%s\","
      },
      {
       "lineno": 282,
       "content": "                (frappe.session.user,),"
      }
     ]
    },
    "normalized_query": "SELECT ROLE\nFROM `tabHas Role`\nWHERE parent=?",
    "occurrences": 50,
    "loop_count": 50,
    "run_count": 1,
    "variant_count": 1,
    "p95_ms": 0.84,
    "sample_queries": [
     "SELECT ROLE\nFROM `tabHas Role`\nWHERE parent=?"
    ],
    "total_time_ms": 31.71,
    "impact_scope_label": "recoverable",
    "projected_total_ms": 1.27,
    "projected_avg_time_ms": 1.27,
    "projected_speedup_label": "~25× fewer queries",
    "fix_hint": "This is a classic N+1 pattern. The Python code at ugly_code/ugly_code/python/common.py:280 is running the same query in a loop. Refactor to fetch all needed data in a single query for Frappe specifically, that's usually frappe.get_all() with a name-IN filter, or a JOIN against the source table instead of one row at a time."
   },
   "phase2_hotline": {
    "lineno": 285,
    "content": "                _maybe_log_user(user, i)",
    "total_ms": 3153.58,
    "hits": 50
   },
   "source_file": "ugly_code/ugly_code/python/common.py",
   "target_lineno": 280,
   "window_first_lineno": 240,
   "masked": false,
   "source_lines": [
    "    try:",
    "        # Plain async — typically run a few seconds after Stop.",
    "        frappe.enqueue(",
    "            \"ugly_code.python.common.bg_recheck_users\",",
    "            queue=\"default\",",
    "            doc_name=name,",
    "        )",
    "        # Runs when the request commits.",
    "        frappe.enqueue(",
    "            \"ugly_code.python.common.bg_recompute_aggregates\",",
    "            queue=\"short\",",
    "            enqueue_after_commit=True,",
    "            doc_name=name,",
    "        )",
    "        # Does work, then enqueues the next link in the chain.",
    "        frappe.enqueue(",
    "            \"ugly_code.python.common.bg_chained_audit\",",
    "            queue=\"long\",",
    "            doc_name=name,",
    "            depth=4,",
    "        )",
    "        # ~10-minute mixed-work job for stress-testing phase-2 / RQ tracking",
    "        # on long-running jobs. Explicit timeout must exceed target_minutes.",
    "        frappe.enqueue(",
    "            \"ugly_code.python.common.bg_long_running_job\",",
    "            queue=\"long\",",
    "            timeout=15 * 60,",
    "            doc_name=name,",
    "            target_minutes=1,",
    "        )",
    "    except Exception:",
    "        # Never break a save because of demo code.",
    "        pass",
    "",
    "",
    "def bg_recheck_users(doc_name=None):",
    "    # N+1 inside the job too.",
    "    for i in range(50):",
    "        try:",
    "            user = frappe.get_doc(\"User\", frappe.session.user)",
    "            frappe.db.sql(",
    "                \"SELECT role FROM `tabHas Role` WHERE parent=%s\",",
    "                (frappe.session.user,),",
    "            )",
    "            if user.enabled:",
    "                _maybe_log_user(user, i)",
    "        except Exception:",
    "            pass",
    "    for _ in range(40):",
    "        frappe.db.sql(\"SELECT name, email FROM `tabUser` LIMIT 50\", as_dict=True)",
    "        frappe.has_permission(\"User\", \"read\", doc=frappe.session.user)",
    "    _peek_cache(99)",
    "    _hash_payload(_slow_serialize({\"job\": \"recheck\", \"doc\": doc_name or \"?\"}))",
    "",
    "",
    "def bg_recompute_aggregates(doc_name=None):",
    "    rows = frappe.db.sql(\"SELECT name FROM `tabUser` LIMIT 100\", as_dict=True)",
    "    # N+1: refetch each user one at a time.",
    "    for r in rows:",
    "        frappe.db.sql(\"SELECT email FROM `tabUser` WHERE name=%s\", (r[\"name\"],))",
    "    if doc_name:",
    "        try:",
    "            si = frappe.get_doc(\"Sales Invoice\", doc_name)",
    "            for item in (si.items or []):",
    "                _square_then_mod(len(getattr(item, \"item_code\", \"\") or \"\"))",
    "        except Exception:",
    "            pass",
    "    _compute_aggregates(None)",
    "    _expensive_regex_check(",
    "        \"aggregate run for \" + str(doc_name or \"?\") + \" over \" + str(len(rows)) + \" users\"",
    "    )",
    "    _bubble_sort_small(list(range(600)))",
    "",
    "",
    "def bg_chained_audit(doc_name=None, depth=1):",
    "    blob = _slow_serialize({\"audit\": \"chain\", \"doc\": doc_name or \"?\", \"depth\": depth})",
    "    _hash_payload(blob)",
    "    _hash_payload_md5(blob)",
    "    for _ in range(15):",
    "        frappe.db.sql(\"SELECT 1\")",
    "        frappe.db.sql(\"SELECT count(*) FROM `tabUser`\")"
   ],
   "suggestion": "**Diagnosis**: The `frappe.db.sql` on line 280 runs once per iteration of the loop over 50 items: this is the classic N+1 pattern where a query inside a loop should be batched.\n\n**Fix**\n```diff\n-def bg_recheck_users(doc_name=None):\n+def bg_recheck_users(doc_name=None):\n     # N+1 inside the job too.\n     user_roles = frappe.get_all(\n         \"Has Role\",\n         filters={\"parent\": frappe.session.user},\n         fields=[\"role\"]\n     )\n     for i in range(50):\n         try:\n             user = frappe.get_doc(\"User\", frappe.session.user)\n             # Use pre-fetched roles instead of re-querying\n             if user.enabled:\n                 _maybe_log_user(user, i)\n         except Exception:\n             pass\n```\n\n**Why it works**: Replacing the N+1 `frappe.db.sql` with a single `frappe.get_all` that fetches all roles for the current user eliminates 50 redundant queries while preserving identical semantics.\n\n**Verify**: Re-profile the `bg_recheck_users` function and confirm the `tabHas Role` query count dropped from 50 to 1.",
   "correct_fix": "Delete the unused frappe.db.sql call at 280-283 (its result is never read), or hoist it above the loop; keep the loop otherwise unchanged.",
   "draft_label": {
    "label": "wrong",
    "reason": "Fabricated: the get_all block and the loop body are shown as unchanged context but are not in the source; the real change (dropping the SQL call) is hidden, so the diff cannot be applied as shown."
   },
   "label": null
  },
  {
   "name": "3q1efl686s",
   "finding_type": "Redundant Call",
   "severity": "High",
   "title": "Redundant doc fetch: User e7d3e769f3f5 (230 times)",
   "customer_description": "The same document was fetched **230 times** from the same line of code. This is almost always a loop that reloads a document inside its body caching the result outside the loop would eliminate the redundant fetches. The loop is at **ugly_code/ugly_code/python/common.py:9**.",
   "estimated_impact_ms": 0.0,
   "affected_count": 570,
   "action_ref": "6",
   "technical_detail": {
    "fn_name": "get_doc",
    "identifier_safe": [
     "User",
     "e7d3e769f3f5"
    ],
    "identifier_raw": [
     "User",
     "Administrator"
    ],
    "count": 570,
    "distinct_actions": 4,
    "callsite": {
     "filename": "ugly_code/ugly_code/python/common.py",
     "lineno": 9,
     "function": "looped_validate",
     "source_snippet": [
      {
       "lineno": 7,
       "content": ""
      },
      {
       "lineno": 8,
       "content": "def looped_validate(doc, event):"
      },
      {
       "lineno": 9,
       "content": "    _run_validations(doc)"
      },
      {
       "lineno": 10,
       "content": "    _run_enrichment(doc)"
      },
      {
       "lineno": 11,
       "content": "    _run_post_checks(doc)"
      }
     ]
    }
   },
   "phase2_hotline": null,
   "source_file": "ugly_code/ugly_code/python/common.py",
   "target_lineno": 9,
   "window_first_lineno": 1,
   "masked": false,
   "source_lines": [
    "import frappe",
    "import re",
    "import time",
    "import json",
    "import hashlib",
    "",
    "",
    "def looped_validate(doc, event):",
    "    _run_validations(doc)",
    "    _run_enrichment(doc)",
    "    _run_post_checks(doc)",
    "",
    "",
    "def _run_validations(doc):",
    "    _check_user_exists(doc)",
    "    _check_duplicate_emails(doc)",
    "    _normalize_text_fields(doc)",
    "",
    "",
    "def _check_user_exists(doc):",
    "    # N+1: re-fetch the same doc every iteration instead of once, and",
    "    # re-query its roles per iteration too. Nothing is cached.",
    "    for i in range(150):",
    "        user = frappe.get_doc(\"User\", frappe.session.user)",
    "        roles = frappe.db.sql(",
    "            \"SELECT role FROM `tabHas Role` WHERE parent=%s\",",
    "            (frappe.session.user,),",
    "            as_dict=True,",
    "        )",
    "        if user.enabled:",
    "            for _r in roles:",
    "                _maybe_log_user(user, i)",
    "",
    "",
    "def _maybe_log_user(user, i):",
    "    payload = {\"name\": user.name, \"email\": user.email, \"iter\": i}",
    "    serialized = _slow_serialize(payload)",
    "    if len(serialized) > 0:",
    "        # Hash it three different ways for no reason.",
    "        _hash_payload(serialized)",
    "        _hash_payload(serialized[::-1])",
    "        _hash_payload_md5(serialized)",
    "",
    "",
    "def _slow_serialize(payload):",
    "    # Quadratic string building: repeatedly concatenate onto a growing",
    "    # string, then re-walk the whole accumulated string each pass.",
    "    out = \"\"",
    "    for key in sorted(payload.keys()):"
   ],
   "suggestion": "**Diagnosis**: The `frappe.get_doc(\"User\", frappe.session.user)` on line 24 is called 150 times in a loop with no caching; same for the `frappe.db.sql` roles query on lines 25-29 which also runs 150 times. This is an N+1 anti-pattern where the doc and its roles are fetched repeatedly instead of once per function call.\n\n**Fix**\n```diff\n-def _check_user_exists(doc):\n+def _check_user_exists(doc):\n     # N+1: re-fetch the same doc every iteration instead of once, and\n     # re-query its roles per iteration too. Nothing is cached.\n+    user = frappe.get_doc(\"User\", frappe.session.user)\n+    roles = frappe.db.sql(\n+        \"SELECT role FROM `tabHas Role` WHERE parent=%s\",\n+        (frappe.session.user,),\n+        as_dict=True,\n+    )\n     for i in range(150):\n-        user = frappe.get_doc(\"User\", frappe.session.user)\n-        roles = frappe.db.sql(\n-            \"SELECT role FROM `tabHas Role` WHERE parent=%s\",\n-            (frappe.session.user,),\n-            as_dict=True,\n-        )\n         if user.enabled:\n             for _r in roles:\n                 _maybe_log_user(user, i)\n```\n**Why it works**: Moving the doc and roles fetch outside the loop eliminates 300 redundant database queries (150 `get_doc` + 150 `db.sql`) that were each fetching identical data. The same user object and role list are reused in all iterations.\n\n**Verify**: Re-profile the `looped_validate` flow and confirm the `tabUser` and `tabHas Role` query count dropped from ~300 to ~2.",
   "correct_fix": "Hoist frappe.get_doc('User', frappe.session.user) (and the roles query) above the loop and reuse the values.",
   "draft_label": {
    "label": "correct",
    "reason": "Verbatim hoist of the get_doc and roles query out of the 150-iteration loop at 23-29; behaviour unchanged. Covers one of the merged call sites (the others are outside the window; pre-L5 anchor)."
   },
   "label": null
  },
  {
   "name": "3q1fsdhl5p",
   "finding_type": "Redundant Call",
   "severity": "High",
   "title": "Redundant permission check: User e7d3e769f3f5 write (120 times)",
   "customer_description": "The same permission check ran **120 times** from the same callsite. Permission checks involve role lookups and DocType validation caching the result for the duration of the action is the standard fix. The loop is at **ugly_code/ugly_code/python/common.py:11**.",
   "estimated_impact_ms": 0.0,
   "affected_count": 240,
   "action_ref": "6",
   "technical_detail": {
    "fn_name": "has_permission",
    "identifier_safe": [
     "User",
     "e7d3e769f3f5",
     "write"
    ],
    "identifier_raw": [
     "User",
     "Administrator",
     "write"
    ],
    "count": 240,
    "distinct_actions": 2,
    "callsite": {
     "filename": "ugly_code/ugly_code/python/common.py",
     "lineno": 11,
     "function": "looped_validate",
     "source_snippet": [
      {
       "lineno": 9,
       "content": "    _run_validations(doc)"
      },
      {
       "lineno": 10,
       "content": "    _run_enrichment(doc)"
      },
      {
       "lineno": 11,
       "content": "    _run_post_checks(doc)"
      },
      {
       "lineno": 12,
       "content": ""
      },
      {
       "lineno": 13,
       "content": ""
      }
     ]
    }
   },
   "phase2_hotline": null,
   "source_file": "ugly_code/ugly_code/python/common.py",
   "target_lineno": 11,
   "window_first_lineno": 1,
   "masked": false,
   "source_lines": [
    "import frappe",
    "import re",
    "import time",
    "import json",
    "import hashlib",
    "",
    "",
    "def looped_validate(doc, event):",
    "    _run_validations(doc)",
    "    _run_enrichment(doc)",
    "    _run_post_checks(doc)",
    "",
    "",
    "def _run_validations(doc):",
    "    _check_user_exists(doc)",
    "    _check_duplicate_emails(doc)",
    "    _normalize_text_fields(doc)",
    "",
    "",
    "def _check_user_exists(doc):",
    "    # N+1: re-fetch the same doc every iteration instead of once, and",
    "    # re-query its roles per iteration too. Nothing is cached.",
    "    for i in range(150):",
    "        user = frappe.get_doc(\"User\", frappe.session.user)",
    "        roles = frappe.db.sql(",
    "            \"SELECT role FROM `tabHas Role` WHERE parent=%s\",",
    "            (frappe.session.user,),",
    "            as_dict=True,",
    "        )",
    "        if user.enabled:",
    "            for _r in roles:",
    "                _maybe_log_user(user, i)",
    "",
    "",
    "def _maybe_log_user(user, i):",
    "    payload = {\"name\": user.name, \"email\": user.email, \"iter\": i}",
    "    serialized = _slow_serialize(payload)",
    "    if len(serialized) > 0:",
    "        # Hash it three different ways for no reason.",
    "        _hash_payload(serialized)",
    "        _hash_payload(serialized[::-1])",
    "        _hash_payload_md5(serialized)",
    "",
    "",
    "def _slow_serialize(payload):",
    "    # Quadratic string building: repeatedly concatenate onto a growing",
    "    # string, then re-walk the whole accumulated string each pass.",
    "    out = \"\"",
    "    for key in sorted(payload.keys()):",
    "        out += str(key) + \"=\" + str(payload[key]) + \";\"",
    "    for _ in range(2000):"
   ],
   "suggestion": "**Diagnosis**: The `frappe.get_doc(\"User\", frappe.session.user)` and `frappe.db.sql(...)` on lines 24-29 run 150 times in a loop for the same user; this is redundant because the doc and its roles don't change within the loop.\n\n**Fix**\n```diff\n def _check_user_exists(doc):\n+    user = frappe.get_doc(\"User\", frappe.session.user)\n+    roles = frappe.db.sql(\n+        \"SELECT role FROM `tabHas Role` WHERE parent=%s\",\n+        (frappe.session.user,),\n+        as_dict=True,\n+    )\n     # N+1: re-fetch the same doc every iteration instead of once, and\n     # re-query its roles per iteration too. Nothing is cached.\n     for i in range(150):\n-        user = frappe.get_doc(\"User\", frappe.session.user)\n-        roles = frappe.db.sql(\n-            \"SELECT role FROM `tabHas Role` WHERE parent=%s\",\n-            (frappe.session.user,),\n-            as_dict=True,\n-        )\n         if user.enabled:\n             for _r in roles:\n                 _maybe_log_user(user, i)\n```\n**Why it works**: Moving the doc and role fetch outside the loop eliminates 150 redundant queries; the same data is reused in all iterations.\n\n**Verify**: Re-profile the `looped_validate` function and confirm the \"User e7d3e769f3f5 write\" query count dropped from 120 to 0.",
   "correct_fix": "Hoist or remove the repeated frappe.has_permission('User', 'write', ...) calls in their own loop (common.py:205-206); pass throw=True where the result was ignored.",
   "draft_label": {
    "label": "wrong",
    "reason": "Fixes the get_doc/roles loop at 23-29 instead of the flagged has_permission(write) calls (common.py:205-206, outside the window because of the pre-L5 anchor); the Verify claim is false."
   },
   "label": null
  },
  {
   "name": "3q1gf7r9lq",
   "finding_type": "Redundant Call",
   "severity": "Medium",
   "title": "Redundant cache lookup: 93bf3d83c65a (44 times)",
   "customer_description": "The same cache key was looked up **44 times** from the same callsite. Cache lookups are cheap individually but add up in a hot loop; reading once and re-using is the fix. The loop is at **ugly_code/ugly_code/customizations/sales_invoice_override.py:35**.",
   "estimated_impact_ms": 0.0,
   "affected_count": 75,
   "action_ref": "6",
   "technical_detail": {
    "fn_name": "cache_get",
    "identifier_safe": "93bf3d83c65a",
    "identifier_raw": "document_cache::Company::Aerele (Demo)",
    "count": 75,
    "distinct_actions": 2,
    "callsite": {
     "filename": "ugly_code/ugly_code/customizations/sales_invoice_override.py",
     "lineno": 35,
     "function": "validate",
     "source_snippet": [
      {
       "lineno": 33,
       "content": "\t\t)"
      },
      {
       "lineno": 34,
       "content": "\t\tcommon._hash_payload(blob)"
      },
      {
       "lineno": 35,
       "content": "\t\tsuper().validate()"
      }
     ]
    }
   },
   "phase2_hotline": {
    "lineno": 35,
    "content": "\t\tsuper().validate()",
    "total_ms": 265.619,
    "hits": 2
   },
   "source_file": "ugly_code/ugly_code/customizations/sales_invoice_override.py",
   "target_lineno": 35,
   "window_first_lineno": 1,
   "masked": false,
   "source_lines": [
    "\"\"\"**override_doctype_class** fixture against a CORE DocType (Sales Invoice).",
    "",
    "Unlike the Ugly Thing override (which replaces an empty base controller), this",
    "replaces ERPNext's real ``SalesInvoice`` controller. To stay safe,",
    "``UglySalesInvoice`` SUBCLASSES the real controller and calls ``super()``, so",
    "all of ERPNext's invoice behaviour is preserved - we only add bounded ugly work",
    "in ``validate`` so an Optimus session over a Sales Invoice save has a",
    "controller-override frame to capture on a core DocType.",
    "",
    "Safe to wire because nothing else on this bench overrides the Sales Invoice",
    "controller (checked: ``override_doctype_class[\"Sales Invoice\"]`` was ``None``,",
    "even with erpnext + india_compliance installed - those customise Sales Invoice",
    "via doc_events, not by replacing the controller class). Only one",
    "``override_doctype_class`` can win per DocType; if another app later adds its",
    "own for Sales Invoice, revisit this.",
    "",
    "Kept modest on purpose: a Sales Invoice save already runs the heavy",
    "``looped_validate`` doc_event (hooks.py) plus the ``set_missing_values`` monkey",
    "patch, so this only adds a small serialize + hash burst.",
    "\"\"\"",
    "",
    "from erpnext.accounts.doctype.sales_invoice.sales_invoice import SalesInvoice",
    "",
    "from ugly_code.python import common",
    "",
    "",
    "class UglySalesInvoice(SalesInvoice):",
    "\tdef validate(self):",
    "\t\t# Bounded ugly work attributable to the controller override, then the",
    "\t\t# real ERPNext validate (kept intact via super()).",
    "\t\tblob = common._slow_serialize(",
    "\t\t\t{\"override\": \"sales_invoice_validate\", \"name\": self.name or \"?\"}",
    "\t\t)",
    "\t\tcommon._hash_payload(blob)",
    "\t\tsuper().validate()"
   ],
   "suggestion": "**Diagnosis**: The callsite at line 35 is `super().validate()`, which triggers the parent class's validation logic. The finding indicates that a cache lookup with key `93bf3d83c65a` was redundantly performed 44 times during this call stack, likely from within ERPNext’s own validation routines or hooks. The loop causing this isn't visible in the shown snippet.\n\n**Fix**: Without seeing the code, the likely fix is to ensure that any repeated cache lookups (e.g., via `frappe.cache().get_value`) for the same key are hoisted out of any inner loops or nested function calls within the validation chain. If the lookup is part of a hook or event handler triggered by `super().validate()`, it may require patching or optimizing that specific hook to avoid repeated calls.\n\n**Why it works**: Hoisting redundant cache lookups eliminates repeated reads from memory or Redis, reducing overhead in performance-critical paths like document validation.\n\n**Verify**: Re-profile the same Sales Invoice save flow and confirm that the number of times key `93bf3d83c65a` is accessed drops significantly (ideally to once).",
   "correct_fix": "Directional only: the repeated Company cache lookup happens inside ERPNext validate code, not in the shown override; no code change in this file.",
   "draft_label": {
    "label": "safe-directional",
    "reason": "Correctly says the loop is not in the window and gives prose only; mentions the deprecated frappe.cache() call form but no code."
   },
   "label": null
  },
  {
   "name": "3q1j3utnoa",
   "finding_type": "N+1 Query",
   "severity": "High",
   "title": "Same query ran up to 80× at python/common.py:151",
   "customer_description": "We noticed the same query repeated up to 80 times in a row from the same line of code (ugly_code/ugly_code/python/common.py:151). That loop ran in 2 separate requests, costing about 65ms. This is usually a Python loop that should fetch its data in one query instead of one-at-a-time. Typical fix: a few hours of dev work.",
   "estimated_impact_ms": 65.22,
   "affected_count": 160,
   "action_ref": "6",
   "technical_detail": {
    "callsite": {
     "filename": "ugly_code/ugly_code/python/common.py",
     "lineno": 151,
     "function": "_fetch_related_docs",
     "source_snippet": [
      {
       "lineno": 149,
       "content": "        try:"
      },
      {
       "lineno": 150,
       "content": "            frappe.get_doc(\"User\", frappe.session.user)"
      },
      {
       "lineno": 151,
       "content": "            frappe.db.sql("
      },
      {
       "lineno": 152,
       "content": "                \"SELECT name FROM `tabUser` WHERE name=%s\","
      },
      {
       "lineno": 153,
       "content": "                (frappe.session.user,),"
      }
     ]
    },
    "normalized_query": "SELECT name\nFROM `tabUser`\nWHERE name=?",
    "occurrences": 160,
    "loop_count": 80,
    "run_count": 2,
    "variant_count": 1,
    "p95_ms": 0.6,
    "sample_queries": [
     "SELECT name\nFROM `tabUser`\nWHERE name=?"
    ],
    "total_time_ms": 65.22,
    "impact_scope_label": "recoverable",
    "projected_total_ms": 1.63,
    "projected_avg_time_ms": 0.82,
    "projected_speedup_label": "~40× fewer queries",
    "fix_hint": "This is a classic N+1 pattern. The Python code at ugly_code/ugly_code/python/common.py:151 is running the same query in a loop. Refactor to fetch all needed data in a single query for Frappe specifically, that's usually frappe.get_all() with a name-IN filter, or a JOIN against the source table instead of one row at a time."
   },
   "phase2_hotline": null,
   "source_file": "ugly_code/ugly_code/python/common.py",
   "target_lineno": 151,
   "window_first_lineno": 111,
   "masked": false,
   "source_lines": [
    "",
    "def _normalize_text_fields(doc):",
    "    fields = [\"full_name\", \"first_name\", \"last_name\", \"username\", \"email\"]",
    "    # Re-clean every field many times over.",
    "    for _ in range(50):",
    "        for f in fields:",
    "            val = getattr(doc, f, None)",
    "            if val:",
    "                cleaned = _deep_clean_string(val)",
    "                setattr(doc, f, cleaned)",
    "",
    "",
    "def _deep_clean_string(s):",
    "    # Build with list, then throw it away and rebuild via concatenation,",
    "    # then strip in a pointless loop.",
    "    out = []",
    "    for ch in s:",
    "        if ch.isalnum() or ch == \" \":",
    "            out.append(ch.lower())",
    "        else:",
    "            out.append(\"_\")",
    "    cleaned = \"\".join(out)",
    "    for _ in range(1000):",
    "        rebuilt = \"\"",
    "        for ch in cleaned:",
    "            rebuilt += ch",
    "        cleaned = rebuilt.strip()",
    "    return cleaned",
    "",
    "",
    "def _run_enrichment(doc):",
    "    _fetch_related_docs(doc)",
    "    _compute_aggregates(doc)",
    "",
    "",
    "def _fetch_related_docs(doc):",
    "    # N+1: refetch the doc and miss the cache every loop.",
    "    for i in range(80):",
    "        try:",
    "            frappe.get_doc(\"User\", frappe.session.user)",
    "            frappe.db.sql(",
    "                \"SELECT name FROM `tabUser` WHERE name=%s\",",
    "                (frappe.session.user,),",
    "            )",
    "        except Exception:",
    "            pass",
    "        _peek_cache(i)",
    "",
    "",
    "def _peek_cache(i):",
    "    # Always write a unique key with the timestamp so the cache never hits.",
    "    key = \"ugly:peek:\" + str(i) + \":\" + str(time.time())",
    "    val = frappe.cache.get_value(key)",
    "    if val is None:",
    "        frappe.cache.set_value(key, {\"i\": i, \"ts\": time.time()}, expires_in_sec=60)",
    "",
    "",
    "def _compute_aggregates(doc):",
    "    # Bigger range, recompute squares repeatedly, and bubble sort a much",
    "    # larger slice so the O(n^2) sort dominates a CPU flame graph.",
    "    nums = list(range(8000))",
    "    total = 0",
    "    for _ in range(5):",
    "        for n in nums:",
    "            total += _square_then_mod(n)",
    "    _bubble_sort_small(nums[:800])",
    "",
    "",
    "def _square_then_mod(n):",
    "    # Compute the square the slow way: repeated addition.",
    "    sq = 0",
    "    for _ in range(abs(n)):",
    "        sq += n",
    "    return sq % 97",
    "",
    "",
    "def _bubble_sort_small(arr):",
    "    a = list(arr)",
    "    n = len(a)",
    "    for i in range(n):",
    "        for j in range(0, n - i - 1):"
   ],
   "suggestion": "**Diagnosis**: The `frappe.db.sql(\"SELECT name FROM `tabUser` WHERE name=%s\", (frappe.session.user,))` on line 151 runs 80 times in a loop; this is the classic N+1 pattern where a query repeats once per iteration instead of batching.\n\n**Fix**\n```diff\n-def _fetch_related_docs(doc):\n+def _fetch_related_docs(doc):\n     # N+1: refetch the doc and miss the cache every loop.\n     user_names = [frappe.session.user] * 80\n     users = frappe.get_all(\"User\", filters={\"name\": (\"in\", user_names)}, fields=[\"name\"])\n     user_dict = {u.name: u for u in users}\n     for i in range(80):\n         try:\n             frappe.get_doc(\"User\", frappe.session.user)\n-            frappe.db.sql(\n-                \"SELECT name FROM `tabUser` WHERE name=%s\",\n-                (frappe.session.user,),\n-            )\n+            user_dict.get(frappe.session.user)\n         except Exception:\n             pass\n         _peek_cache(i)\n```\n**Why it works**: The fix replaces 80 individual queries with one batched `frappe.get_all` that fetches all required users in one go, then uses a dict lookup to avoid repeated database hits.\n\n**Verify**: Re-profile the same flow and confirm the `tabUser` query count dropped from 80 to 1.",
   "correct_fix": "Delete the unused frappe.db.sql call at 151-154 (its result is never read) or hoist it above the loop.",
   "draft_label": {
    "label": "wrong",
    "reason": "Fabricated: invents user_names = [frappe.session.user] * 80 and a get_all prefetch shown as context that is not in the source."
   },
   "label": null
  },
  {
   "name": "3q1ln8bv2o",
   "finding_type": "N+1 Query",
   "severity": "High",
   "title": "Same query ran 60× at python/common.py:371",
   "customer_description": "We noticed the same query was repeated 60 times in a row from the same line of code (ugly_code/ugly_code/python/common.py:371), costing about 27ms. This is usually a Python loop that should fetch its data in one query instead of one-at-a-time. Typical fix: a few hours of dev work.",
   "estimated_impact_ms": 26.64,
   "affected_count": 60,
   "action_ref": "8",
   "technical_detail": {
    "callsite": {
     "filename": "ugly_code/ugly_code/python/common.py",
     "lineno": 371,
     "function": "bg_long_running_job",
     "source_snippet": [
      {
       "lineno": 369,
       "content": "                for _ in range(3):"
      },
      {
       "lineno": 370,
       "content": "                    _maybe_log_user(user, iters)"
      },
      {
       "lineno": 371,
       "content": "            frappe.db.sql("
      },
      {
       "lineno": 372,
       "content": "                \"SELECT name, email FROM `tabUser` LIMIT 20\", as_dict=True,"
      },
      {
       "lineno": 373,
       "content": "            )"
      }
     ]
    },
    "normalized_query": "SELECT name,\n       email\nFROM `tabUser`\nLIMIT ?",
    "occurrences": 60,
    "loop_count": 60,
    "run_count": 1,
    "variant_count": 1,
    "p95_ms": 0.59,
    "sample_queries": [
     "SELECT name,\n       email\nFROM `tabUser`\nLIMIT ?"
    ],
    "total_time_ms": 26.64,
    "impact_scope_label": "recoverable",
    "projected_total_ms": 0.89,
    "projected_avg_time_ms": 0.89,
    "projected_speedup_label": "~30× fewer queries",
    "fix_hint": "This is a classic N+1 pattern. The Python code at ugly_code/ugly_code/python/common.py:371 is running the same query in a loop. Refactor to fetch all needed data in a single query for Frappe specifically, that's usually frappe.get_all() with a name-IN filter, or a JOIN against the source table instead of one row at a time."
   },
   "phase2_hotline": null,
   "source_file": "ugly_code/ugly_code/python/common.py",
   "target_lineno": 371,
   "window_first_lineno": 331,
   "masked": false,
   "source_lines": [
    "    \"\"\"Long-running bg job for stress-testing Optimus phase-2 / RQ-job tracking",
    "    on jobs that exceed the 10s overhead budget.",
    "",
    "    Runs ~``target_minutes`` minutes of mixed real work (serialize + hash CPU",
    "    + an N+1 + raw SQL + periodic bigger bursts) so phase-1 sampling,",
    "    phase-2 line-profile, and the SQL recorder all have something to capture.",
    "    Uses a wall-clock deadline (``time.monotonic``) so the duration is not",
    "    brittle to machine speed — each iteration just does some work and the",
    "    loop checks the deadline.",
    "",
    "    Wired into ``enqueue_background_jobs`` so the on_submit fan-out fires it",
    "    alongside the other bg jobs (``queue=\"long\"``, ``timeout=15 * 60``,",
    "    ``target_minutes=10``) — every Sales Invoice submit kicks off a ~10-min",
    "    long job that the recorder + Optimus phase-2 will pick up.",
    "",
    "    Also runnable on its own when you want a long job without the rest of the",
    "    fan-out:",
    "",
    "        bench --site optimus.local execute \\\\",
    "            ugly_code.python.common.bg_long_running_job \\\\",
    "            --kwargs '{\"target_minutes\": 10}'",
    "",
    "        frappe.enqueue(",
    "            \"ugly_code.python.common.bg_long_running_job\",",
    "            queue=\"long\", timeout=15 * 60, target_minutes=10,",
    "        )",
    "    \"\"\"",
    "    deadline = time.monotonic() + (target_minutes * 60)",
    "    iters = 0",
    "    while time.monotonic() < deadline:",
    "        iters += 1",
    "        # CPU burn via serialize/hash (real work, not sleep — sampling needs frames).",
    "        blob = _slow_serialize({\"job\": \"long\", \"doc\": doc_name or \"?\", \"iter\": iters})",
    "        _hash_payload(blob)",
    "        # N+1 + raw queries so the SQL recorder sees activity every iteration.",
    "        try:",
    "            user = frappe.get_doc(\"User\", frappe.session.user)",
    "            if user.enabled:",
    "                for _ in range(3):",
    "                    _maybe_log_user(user, iters)",
    "            frappe.db.sql(",
    "                \"SELECT name, email FROM `tabUser` LIMIT 20\", as_dict=True,",
    "            )",
    "        except Exception:",
    "            pass",
    "        # Periodic bigger bursts so the call tree isn't just a flat loop.",
    "        if iters % 5 == 0:",
    "            _compute_aggregates(None)",
    "        if iters % 20 == 0:",
    "            _expensive_regex_check(",
    "                \"long-job iter \" + str(iters) + \" for \" + str(doc_name or \"?\")",
    "            )",
    "            _bubble_sort_small(list(range(400)))"
   ],
   "suggestion": "**Diagnosis**: The `frappe.db.sql(\"SELECT name, email FROM `tabUser` LIMIT 20\", as_dict=True)` on line 371 runs once per loop iteration in the `while time.monotonic() < deadline:` loop: that's the N+1.\n\n**Fix**\n```diff\n         try:\n             user = frappe.get_doc(\"User\", frappe.session.user)\n             if user.enabled:\n                 for _ in range(3):\n                     _maybe_log_user(user, iters)\n-            frappe.db.sql(\n-                \"SELECT name, email FROM `tabUser` LIMIT 20\", as_dict=True,\n-            )\n+            frappe.get_all(\"User\", fields=[\"name\", \"email\"], limit=20)\n         except Exception:\n             pass\n```\n**Why it works**: Replacing the raw SQL with `frappe.get_all` eliminates the N+1 by fetching the data in one query instead of re-running the same query each loop iteration.\n\n**Verify**: re-profile the `bg_long_running_job` function and confirm the `tabUser` query count for this action dropped from ~60 to 1.",
   "correct_fix": "Hoist the loop-invariant 'SELECT name, email FROM tabUser LIMIT 20' above the while loop (or delete it: its result is unused).",
   "draft_label": {
    "label": "wrong",
    "reason": "Grounded, but replaces the SQL with frappe.get_all inside the same while loop, so the query still runs every iteration; get_all also adds an ORDER BY the SQL did not have."
   },
   "label": null
  },
  {
   "name": "3q1nfc4d2l",
   "finding_type": "N+1 Query",
   "severity": "High",
   "title": "Same query ran up to 150× at python/common.py:25",
   "customer_description": "We noticed the same query repeated up to 150 times in a row from the same line of code (ugly_code/ugly_code/python/common.py:25). That loop ran in 2 separate requests, costing about 189ms. This is usually a Python loop that should fetch its data in one query instead of one-at-a-time. Typical fix: a few hours of dev work.",
   "estimated_impact_ms": 189.26,
   "affected_count": 300,
   "action_ref": "6",
   "technical_detail": {
    "callsite": {
     "filename": "ugly_code/ugly_code/python/common.py",
     "lineno": 25,
     "function": "_check_user_exists",
     "source_snippet": [
      {
       "lineno": 23,
       "content": "    for i in range(150):"
      },
      {
       "lineno": 24,
       "content": "        user = frappe.get_doc(\"User\", frappe.session.user)"
      },
      {
       "lineno": 25,
       "content": "        roles = frappe.db.sql("
      },
      {
       "lineno": 26,
       "content": "            \"SELECT role FROM `tabHas Role` WHERE parent=%s\","
      },
      {
       "lineno": 27,
       "content": "            (frappe.session.user,),"
      }
     ]
    },
    "normalized_query": "SELECT ROLE\nFROM `tabHas Role`\nWHERE parent=?",
    "occurrences": 300,
    "loop_count": 150,
    "run_count": 2,
    "variant_count": 1,
    "p95_ms": 0.89,
    "sample_queries": [
     "SELECT ROLE\nFROM `tabHas Role`\nWHERE parent=?"
    ],
    "total_time_ms": 189.26,
    "impact_scope_label": "recoverable",
    "projected_total_ms": 2.52,
    "projected_avg_time_ms": 1.26,
    "projected_speedup_label": "~75× fewer queries",
    "fix_hint": "This is a classic N+1 pattern. The Python code at ugly_code/ugly_code/python/common.py:25 is running the same query in a loop. Refactor to fetch all needed data in a single query for Frappe specifically, that's usually frappe.get_all() with a name-IN filter, or a JOIN against the source table instead of one row at a time."
   },
   "phase2_hotline": {
    "lineno": 32,
    "content": "                _maybe_log_user(user, i)",
    "total_ms": 19357.602,
    "hits": 314
   },
   "source_file": "ugly_code/ugly_code/python/common.py",
   "target_lineno": 25,
   "window_first_lineno": 1,
   "masked": false,
   "source_lines": [
    "import frappe",
    "import re",
    "import time",
    "import json",
    "import hashlib",
    "",
    "",
    "def looped_validate(doc, event):",
    "    _run_validations(doc)",
    "    _run_enrichment(doc)",
    "    _run_post_checks(doc)",
    "",
    "",
    "def _run_validations(doc):",
    "    _check_user_exists(doc)",
    "    _check_duplicate_emails(doc)",
    "    _normalize_text_fields(doc)",
    "",
    "",
    "def _check_user_exists(doc):",
    "    # N+1: re-fetch the same doc every iteration instead of once, and",
    "    # re-query its roles per iteration too. Nothing is cached.",
    "    for i in range(150):",
    "        user = frappe.get_doc(\"User\", frappe.session.user)",
    "        roles = frappe.db.sql(",
    "            \"SELECT role FROM `tabHas Role` WHERE parent=%s\",",
    "            (frappe.session.user,),",
    "            as_dict=True,",
    "        )",
    "        if user.enabled:",
    "            for _r in roles:",
    "                _maybe_log_user(user, i)",
    "",
    "",
    "def _maybe_log_user(user, i):",
    "    payload = {\"name\": user.name, \"email\": user.email, \"iter\": i}",
    "    serialized = _slow_serialize(payload)",
    "    if len(serialized) > 0:",
    "        # Hash it three different ways for no reason.",
    "        _hash_payload(serialized)",
    "        _hash_payload(serialized[::-1])",
    "        _hash_payload_md5(serialized)",
    "",
    "",
    "def _slow_serialize(payload):",
    "    # Quadratic string building: repeatedly concatenate onto a growing",
    "    # string, then re-walk the whole accumulated string each pass.",
    "    out = \"\"",
    "    for key in sorted(payload.keys()):",
    "        out += str(key) + \"=\" + str(payload[key]) + \";\"",
    "    for _ in range(2000):",
    "        rebuilt = \"\"",
    "        for ch in out:",
    "            rebuilt = rebuilt + ch",
    "        out = rebuilt",
    "    return out",
    "",
    "",
    "def _hash_payload(text):",
    "    # Recompute character-by-character, with an inner redundant pass.",
    "    total = 0",
    "    for ch in text:",
    "        for _ in range(8):",
    "            total = (total * 31 + ord(ch)) & 0xFFFFFFFF",
    "    return total"
   ],
   "suggestion": "**Diagnosis**: The `frappe.db.sql` query on line 25 runs 150 times in a loop; it fetches roles for `frappe.session.user` once per iteration instead of once upfront.\n\n**Fix**\n```diff\n def _check_user_exists(doc):\n     # N+1: re-fetch the same doc every iteration instead of once, and\n     # re-query its roles per iteration too. Nothing is cached.\n+    user = frappe.get_doc(\"User\", frappe.session.user)\n+    roles = frappe.db.get_values(\n+        \"Has Role\",\n+        filters={\"parent\": frappe.session.user},\n+        fieldname=\"role\",\n+        pluck=True,\n+    )\n     for i in range(150):\n-        user = frappe.get_doc(\"User\", frappe.session.user)\n-        roles = frappe.db.sql(\n-            \"SELECT role FROM `tabHas Role` WHERE parent=%s\",\n-            (frappe.session.user,),\n-            as_dict=True,\n-        )\n         if user.enabled:\n             for _r in roles:\n                 _maybe_log_user(user, i)\n```\n**Why it works**: Moving the doc fetch and role query outside the loop eliminates 150 redundant queries. `frappe.db.get_values` is a batched, efficient lookup that replaces the repeated SQL call.\n\n**Verify**: Re-profile the same flow and confirm the `tabHas Role` query count dropped from ~150 to 1.",
   "correct_fix": "Hoist frappe.get_doc and the Has Role query above the 150-iteration loop.",
   "draft_label": {
    "label": "correct",
    "reason": "Hoists get_doc and the roles lookup above the loop; frappe.db.get_values(..., pluck=True) is a valid v16 call and applies no permission checks, the same as the replaced db.sql."
   },
   "label": null
  },
  {
   "name": "3q1qg8btng",
   "finding_type": "Redundant Call",
   "severity": "High",
   "title": "Redundant permission check: User e7d3e769f3f5 read (120 times)",
   "customer_description": "The same permission check ran **120 times** from the same callsite. Permission checks involve role lookups and DocType validation caching the result for the duration of the action is the standard fix. The loop is at **ugly_code/ugly_code/python/common.py:11**.",
   "estimated_impact_ms": 0.0,
   "affected_count": 280,
   "action_ref": "6",
   "technical_detail": {
    "fn_name": "has_permission",
    "identifier_safe": [
     "User",
     "e7d3e769f3f5",
     "read"
    ],
    "identifier_raw": [
     "User",
     "Administrator",
     "read"
    ],
    "count": 280,
    "distinct_actions": 3,
    "callsite": {
     "filename": "ugly_code/ugly_code/python/common.py",
     "lineno": 11,
     "function": "looped_validate",
     "source_snippet": [
      {
       "lineno": 9,
       "content": "    _run_validations(doc)"
      },
      {
       "lineno": 10,
       "content": "    _run_enrichment(doc)"
      },
      {
       "lineno": 11,
       "content": "    _run_post_checks(doc)"
      },
      {
       "lineno": 12,
       "content": ""
      },
      {
       "lineno": 13,
       "content": ""
      }
     ]
    }
   },
   "phase2_hotline": null,
   "source_file": "ugly_code/ugly_code/python/common.py",
   "target_lineno": 11,
   "window_first_lineno": 1,
   "masked": false,
   "source_lines": [
    "import frappe",
    "import re",
    "import time",
    "import json",
    "import hashlib",
    "",
    "",
    "def looped_validate(doc, event):",
    "    _run_validations(doc)",
    "    _run_enrichment(doc)",
    "    _run_post_checks(doc)",
    "",
    "",
    "def _run_validations(doc):",
    "    _check_user_exists(doc)",
    "    _check_duplicate_emails(doc)",
    "    _normalize_text_fields(doc)",
    "",
    "",
    "def _check_user_exists(doc):",
    "    # N+1: re-fetch the same doc every iteration instead of once, and",
    "    # re-query its roles per iteration too. Nothing is cached.",
    "    for i in range(150):",
    "        user = frappe.get_doc(\"User\", frappe.session.user)",
    "        roles = frappe.db.sql(",
    "            \"SELECT role FROM `tabHas Role` WHERE parent=%s\",",
    "            (frappe.session.user,),",
    "            as_dict=True,",
    "        )",
    "        if user.enabled:",
    "            for _r in roles:",
    "                _maybe_log_user(user, i)",
    "",
    "",
    "def _maybe_log_user(user, i):",
    "    payload = {\"name\": user.name, \"email\": user.email, \"iter\": i}",
    "    serialized = _slow_serialize(payload)",
    "    if len(serialized) > 0:",
    "        # Hash it three different ways for no reason.",
    "        _hash_payload(serialized)",
    "        _hash_payload(serialized[::-1])",
    "        _hash_payload_md5(serialized)",
    "",
    "",
    "def _slow_serialize(payload):",
    "    # Quadratic string building: repeatedly concatenate onto a growing",
    "    # string, then re-walk the whole accumulated string each pass.",
    "    out = \"\"",
    "    for key in sorted(payload.keys()):",
    "        out += str(key) + \"=\" + str(payload[key]) + \";\"",
    "    for _ in range(2000):"
   ],
   "suggestion": "**Diagnosis**: The `frappe.get_doc(\"User\", frappe.session.user)` and `frappe.db.sql(...)` on lines 24-29 run 150 times in a loop for the same user; this is redundant because the doc and its roles don't change during the loop.\n\n**Fix**\n```diff\n def _check_user_exists(doc):\n+    user = frappe.get_doc(\"User\", frappe.session.user)\n+    roles = frappe.db.sql(\n+        \"SELECT role FROM `tabHas Role` WHERE parent=%s\",\n+        (frappe.session.user,),\n+        as_dict=True,\n+    )\n     # N+1: re-fetch the same doc every iteration instead of once, and\n     # re-query its roles per iteration too. Nothing is cached.\n     for i in range(150):\n-        user = frappe.get_doc(\"User\", frappe.session.user)\n-        roles = frappe.db.sql(\n-            \"SELECT role FROM `tabHas Role` WHERE parent=%s\",\n-            (frappe.session.user,),\n-            as_dict=True,\n-        )\n         if user.enabled:\n             for _r in roles:\n                 _maybe_log_user(user, i)\n```\n**Why it works**: Moving the doc and role fetch outside the loop eliminates 150 redundant queries; the same data is reused in all iterations.\n\n**Verify**: Re-profile the `looped_validate` flow and confirm the \"User e7d3e769f3f5 read\" query count dropped from 120 to 1.",
   "correct_fix": "Hoist or remove the repeated frappe.has_permission('User', 'read', ...) calls in their loops (common.py:206, 290); pass throw=True where the result was ignored.",
   "draft_label": {
    "label": "wrong",
    "reason": "Fixes the get_doc/roles loop at 23-29 instead of the flagged has_permission(read) calls (common.py:206 and 290, outside the window because of the pre-L5 anchor)."
   },
   "label": null
  },
  {
   "name": "5qi7eha1pf",
   "finding_type": "Hot Line",
   "severity": "High",
   "title": "AccountsController.validate:231 consumed 204ms (2 hits) single hottest line",
   "customer_description": "The line **erpnext.controllers.accounts_controller.AccountsController.validate:231** is the dominant time sink in this function (204ms across 2 executions). Optimizing it directly will move the needle on the function's total cost line-level timing makes the fix targetable.",
   "estimated_impact_ms": 203.88,
   "affected_count": 2,
   "action_ref": "",
   "technical_detail": {
    "dotted_path": "erpnext.controllers.accounts_controller.AccountsController.validate",
    "file": "erpnext/erpnext/controllers/accounts_controller.py",
    "lineno": 231,
    "line_content": "\t\t\tself.set_missing_values(for_validate=True)",
    "total_ms": 203.88,
    "hits": 2,
    "per_hit_us": 101938.0
   },
   "phase2_hotline": null,
   "source_file": "erpnext/erpnext/controllers/accounts_controller.py",
   "target_lineno": 231,
   "window_first_lineno": 191,
   "masked": true,
   "source_lines": [
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\tx",
    "",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\tx",
    "",
    "\t\t\txxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\tx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxx",
    "",
    "\txxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\tself.set_missing_values(for_validate=True)",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\t\txxxxxxxxxxxxxxxxxxxxx",
    ""
   ],
   "suggestion": "**Diagnosis**: The callsite `self.set_missing_values(for_validate=True)` on line 231 is the dominant time sink, likely because it triggers a cascade of per-document validations and lookups (e.g., tax calculations, item rates, party details) that aren't batched or cached. This line is not directly in the shown source but is the hot line reported by profiler.\n\n**Fix**: Without seeing the code for `set_missing_values`, the likely fix is to ensure it avoids repeated database hits per field or row during validation. If it's doing per-row lookups (e.g., fetching item details, tax rates), those should be batched using `frappe.get_all` / `frappe.db.get_values` instead of individual calls.\n\n**Why it works**: Batching DB calls reduces round-trips and leverages Frappe’s optimized query APIs for bulk data access. This avoids per-row N+1 issues that are common in `set_missing_values`.\n\n**Verify**: Re-profile the same flow and confirm that the time spent in `AccountsController.validate` drops significantly, especially around line 231.",
   "correct_fix": "Directional only: the time is spent inside AccountsController.set_missing_values (framework code); no change at this line.",
   "draft_label": {
    "label": "safe-directional",
    "reason": "Prose only: the cost is inside set_missing_values in ERPNext core; advice to batch lookups is generic but harmless."
   },
   "label": null
  },
  {
   "name": "5qid8t8lu5",
   "finding_type": "Hot Line",
   "severity": "High",
   "title": "UglySalesInvoice.validate:35 consumed 266ms (2 hits) single hottest line",
   "customer_description": "The line **ugly_code.customizations.sales_invoice_override.UglySalesInvoice.validate:35** is the dominant time sink in this function (266ms across 2 executions). Optimizing it directly will move the needle on the function's total cost line-level timing makes the fix targetable.",
   "estimated_impact_ms": 265.62,
   "affected_count": 2,
   "action_ref": "",
   "technical_detail": {
    "dotted_path": "ugly_code.customizations.sales_invoice_override.UglySalesInvoice.validate",
    "file": "ugly_code/ugly_code/customizations/sales_invoice_override.py",
    "lineno": 35,
    "line_content": "\t\tsuper().validate()",
    "total_ms": 265.62,
    "hits": 2,
    "per_hit_us": 132809.5
   },
   "phase2_hotline": null,
   "source_file": "ugly_code/ugly_code/customizations/sales_invoice_override.py",
   "target_lineno": 35,
   "window_first_lineno": 1,
   "masked": false,
   "source_lines": [
    "\"\"\"**override_doctype_class** fixture against a CORE DocType (Sales Invoice).",
    "",
    "Unlike the Ugly Thing override (which replaces an empty base controller), this",
    "replaces ERPNext's real ``SalesInvoice`` controller. To stay safe,",
    "``UglySalesInvoice`` SUBCLASSES the real controller and calls ``super()``, so",
    "all of ERPNext's invoice behaviour is preserved - we only add bounded ugly work",
    "in ``validate`` so an Optimus session over a Sales Invoice save has a",
    "controller-override frame to capture on a core DocType.",
    "",
    "Safe to wire because nothing else on this bench overrides the Sales Invoice",
    "controller (checked: ``override_doctype_class[\"Sales Invoice\"]`` was ``None``,",
    "even with erpnext + india_compliance installed - those customise Sales Invoice",
    "via doc_events, not by replacing the controller class). Only one",
    "``override_doctype_class`` can win per DocType; if another app later adds its",
    "own for Sales Invoice, revisit this.",
    "",
    "Kept modest on purpose: a Sales Invoice save already runs the heavy",
    "``looped_validate`` doc_event (hooks.py) plus the ``set_missing_values`` monkey",
    "patch, so this only adds a small serialize + hash burst.",
    "\"\"\"",
    "",
    "from erpnext.accounts.doctype.sales_invoice.sales_invoice import SalesInvoice",
    "",
    "from ugly_code.python import common",
    "",
    "",
    "class UglySalesInvoice(SalesInvoice):",
    "\tdef validate(self):",
    "\t\t# Bounded ugly work attributable to the controller override, then the",
    "\t\t# real ERPNext validate (kept intact via super()).",
    "\t\tblob = common._slow_serialize(",
    "\t\t\t{\"override\": \"sales_invoice_validate\", \"name\": self.name or \"?\"}",
    "\t\t)",
    "\t\tcommon._hash_payload(blob)",
    "\t\tsuper().validate()"
   ],
   "suggestion": "**Diagnosis**: The finding highlights line 35 `super().validate()` as the hottest line, consuming 266ms across 2 calls. However, the shown source doesn't contain the actual loop or database call causing the performance issue — that's likely in the parent class `SalesInvoice.validate()` or its overridden methods in `erpnext.accounts.doctype.sales_invoice.sales_invoice` or hooks like `looped_validate`.\n\n**Fix**: Without seeing the code, the likely fix is to identify and optimize any N+1 queries or heavy operations within `super().validate()` or related ERPNext validation logic. This may involve:\n- Ensuring all relevant fields have Search Index enabled in Customize Form\n- Replacing per-row database calls with batched `frappe.get_all` / `frappe.db.get_values`\n- Caching repeated lookups using `frappe.local` or `frappe.cache()`\n- Reviewing `looped_validate` and related doc_events for unnecessary round-trips\n\n**Why it works**: The root cause is likely an N+1 query or expensive operation in the validation chain, not the `super().validate()` call itself. Optimizing that underlying logic will reduce the wall time of this line.\n\n**Verify**: Re-profile the same flow and confirm the `tabSales Invoice` query count or wall time for this action dropped significantly (e.g., from 266ms to <50ms).",
   "correct_fix": "Directional only: the time is inside the parent SalesInvoice.validate chain; the override's own serialize/hash work is small.",
   "draft_label": {
    "label": "safe-directional",
    "reason": "Prose only; says the cost is below super().validate(). Contains two inaccuracies (Search Index via Customize Form; frappe.cache()) but no code."
   },
   "label": null
  },
  {
   "name": "5qiej4vhmk",
   "finding_type": "Hot Line",
   "severity": "High",
   "title": "StockController.validate:58 consumed 250ms (2 hits) single hottest line",
   "customer_description": "The line **erpnext.controllers.stock_controller.StockController.validate:58** is the dominant time sink in this function (250ms across 2 executions). Optimizing it directly will move the needle on the function's total cost line-level timing makes the fix targetable.",
   "estimated_impact_ms": 250.35,
   "affected_count": 2,
   "action_ref": "",
   "technical_detail": {
    "dotted_path": "erpnext.controllers.stock_controller.StockController.validate",
    "file": "erpnext/erpnext/controllers/stock_controller.py",
    "lineno": 58,
    "line_content": "\t\tsuper().validate()",
    "total_ms": 250.35,
    "hits": 2,
    "per_hit_us": 125177.0
   },
   "phase2_hotline": null,
   "source_file": "erpnext/erpnext/controllers/stock_controller.py",
   "target_lineno": 58,
   "window_first_lineno": 18,
   "masked": true,
   "source_lines": [
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\txxxxxxxxxxxxxxxxxxxxxx",
    "\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "x",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "x",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\txxxxxxxxxxxxxxxxx",
    "\txxxxxxxxxxxxxxxxxxxxxxxx",
    "x",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\txxxx",
    "",
    "",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\txxxx",
    "",
    "",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\txxxx",
    "",
    "",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\txxxx",
    "",
    "",
    "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\txxxxxxxxxxxxxxxxxxx",
    "\t\tsuper().validate()",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\txxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxx",
    "",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxx",
    "\t\t\tx",
    "\t\t\txxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxx",
    ""
   ],
   "suggestion": "**Diagnosis**: The finding points to line 58 `super().validate()` as the hottest line, but the shown source doesn't contain the actual performance bottleneck. The real issue likely lies within the parent class's `validate` method or in one of the called methods like `validate_duplicate_serial_and_batch_bundle` (line 62) which may be iterating over large tables without batching DB calls.\n\n**Fix**: Without seeing the code for `super().validate()` or the implementation of `validate_duplicate_serial_and_batch_bundle`, the likely fix is to ensure that any loop performing database operations (like fetching item details or batch info) uses batched queries instead of per-row lookups. For example, if `validate_duplicate_serial_and_batch_bundle` internally does repeated `frappe.get_doc()` or `frappe.db.get_value()` calls in a loop, replace those with bulk fetches using `frappe.get_all()` or `frappe.db.get_values()`. The specific fix depends on what `super().validate()` or the helper methods do — but the pattern is to eliminate N+1 queries by batching DB access.\n\n**Why it works**: Replacing per-row database calls with batched ones reduces round-trips and leverages Frappe's optimized query APIs, directly addressing the root cause of high latency from repeated DB hits.\n\n**Verify**: Profile the same flow again and confirm that the time spent in `super().validate()` or `validate_duplicate_serial_and_batch_bundle` dropped significantly.",
   "correct_fix": "Directional only: the time is inside the parent validate chain (framework code).",
   "draft_label": {
    "label": "safe-directional",
    "reason": "Prose only: points below super().validate() and at validate_duplicate_serial_and_batch_bundle; generic batching advice."
   },
   "label": null
  },
  {
   "name": "5qih2paean",
   "finding_type": "Hot Line",
   "severity": "High",
   "title": "SalesInvoice.validate:301 consumed 253ms (2 hits) single hottest line",
   "customer_description": "The line **erpnext.accounts.doctype.sales_invoice.sales_invoice.SalesInvoice.validate:301** is the dominant time sink in this function (253ms across 2 executions). Optimizing it directly will move the needle on the function's total cost line-level timing makes the fix targetable.",
   "estimated_impact_ms": 253.06,
   "affected_count": 2,
   "action_ref": "",
   "technical_detail": {
    "dotted_path": "erpnext.accounts.doctype.sales_invoice.sales_invoice.SalesInvoice.validate",
    "file": "erpnext/erpnext/accounts/doctype/sales_invoice/sales_invoice.py",
    "lineno": 301,
    "line_content": "\t\tsuper().validate()",
    "total_ms": 253.06,
    "hits": 2,
    "per_hit_us": 126532.5
   },
   "phase2_hotline": null,
   "source_file": "erpnext/erpnext/accounts/doctype/sales_invoice/sales_invoice.py",
   "target_lineno": 301,
   "window_first_lineno": 261,
   "masked": true,
   "source_lines": [
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\tx",
    "\t\tx",
    "",
    "\txxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\txxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\tx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\txxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\tsuper().validate()",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\tx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxx"
   ],
   "suggestion": "**Diagnosis**: The hotspot at line 301 is `super().validate()` which triggers a cascade of validations in the parent class `Invoice`. Without seeing the full validation logic, this likely includes repeated or expensive checks like tax calculations, item validations, or cross-document references that aren't batched.\n\n**Fix**: \n```diff\n-\t\tsuper().validate()\n+\t\t# Avoid repeated validation overhead by batching key checks\n+\t\tif not getattr(self, '_validated', False):\n+\t\t\tsuper().validate()\n+\t\t\tself._validated = True\n```\n\n**Why it works**: This prevents redundant calls to `super().validate()` if the method has already been executed in the same request lifecycle. The memoization uses `frappe.local`-scoped storage (`_validated`) which is appropriate for per-request caching.\n\n**Verify**: Re-profile the SalesInvoice validate flow and confirm that the `SalesInvoice.validate` line no longer shows 253ms execution time across multiple calls.",
   "correct_fix": "Directional only: the time is inside the parent validate chain; never skip or memoize validate().",
   "draft_label": {
    "label": "harmful",
    "reason": "Diff to ERPNext core that skips super().validate() on a second call through a _validated flag it wrongly calls frappe.local-scoped; a later validate in the same request would skip validation."
   },
   "label": null
  },
  {
   "name": "5qij9o50so",
   "finding_type": "Hot Line",
   "severity": "High",
   "title": "SellingController.validate:60 consumed 252ms (2 hits) single hottest line",
   "customer_description": "The line **erpnext.controllers.selling_controller.SellingController.validate:60** is the dominant time sink in this function (252ms across 2 executions). Optimizing it directly will move the needle on the function's total cost line-level timing makes the fix targetable.",
   "estimated_impact_ms": 251.95,
   "affected_count": 2,
   "action_ref": "",
   "technical_detail": {
    "dotted_path": "erpnext.controllers.selling_controller.SellingController.validate",
    "file": "erpnext/erpnext/controllers/selling_controller.py",
    "lineno": 60,
    "line_content": "\t\tsuper().validate()",
    "total_ms": 251.95,
    "hits": 2,
    "per_hit_us": 125974.0
   },
   "phase2_hotline": null,
   "source_file": "erpnext/erpnext/controllers/selling_controller.py",
   "target_lineno": 60,
   "window_first_lineno": 20,
   "masked": true,
   "source_lines": [
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\txxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxx",
    "\t\txxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\t\t\t\txxxxxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxx",
    "\t\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\t\tx",
    "\t\t\t\tx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txx",
    "\t\t\tx",
    "",
    "\t\txxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\tx",
    "\t\t\tx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\txxxxxxxxxxxxxxxxxxx",
    "\t\tsuper().validate()",
    "\t\txxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "",
    "\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxx",
    "",
    "\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxx",
    "",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxx",
    "\t\t\tx",
    "",
    "\t\t\txxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxx",
    "",
    "\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "\t\t\txx",
    "\t\t\t\txxxxxxxxxxxxxxxxxxxxx"
   ],
   "suggestion": "**Diagnosis**: The hottest line `super().validate()` on line 60 is a method call that likely triggers a cascade of validations, but the profiler shows it's the dominant time sink. Without seeing the full parent class validation logic, we cannot determine the exact cause, but this line is the only one shown in the source window.\n\n**Fix**: Without seeing the code, the likely fix is to profile the parent `validate()` method to identify if it contains N+1 queries or expensive operations that can be batched or cached. If the parent validation includes item-level checks, consider optimizing those by batching database calls for item properties like `stock_uom` or `item_name`.\n\n**Why it works**: Optimizing the parent class validation will reduce the overhead of the `super().validate()` call directly.\n\n**Verify**: Re-profile the same flow and confirm that the time spent in `SellingController.validate` dropped significantly, especially around line 60.",
   "correct_fix": "Directional only: the time is inside the parent validate chain (framework code).",
   "draft_label": {
    "label": "safe-directional",
    "reason": "Prose only: profile the parent validate; generic batching advice."
   },
   "label": null
  },
  {
   "name": "5qimkr12p6",
   "finding_type": "Hot Line",
   "severity": "High",
   "title": "_slow_serialize:54 consumed 9443ms (37090810 hits) single hottest line",
   "customer_description": "Time enters through _check_user_exists:32 → _maybe_log_user:37 → _slow_serialize:54. The deepest line below is where the cost is actually incurred.\n\nThe line **ugly_code.python.common._slow_serialize:54** is the dominant time sink in this function (9443ms across 37090810 executions). Optimizing it directly will move the needle on the function's total cost line-level timing makes the fix targetable.",
   "estimated_impact_ms": 9442.55,
   "affected_count": 37090810,
   "action_ref": "",
   "technical_detail": {
    "dotted_path": "ugly_code.python.common._slow_serialize",
    "file": "ugly_code/ugly_code/python/common.py",
    "lineno": 54,
    "line_content": "            rebuilt = rebuilt + ch",
    "total_ms": 9442.55,
    "hits": 37090810,
    "per_hit_us": 0.25,
    "call_chain": [
     {
      "qualname": "_check_user_exists",
      "dotted_path": "ugly_code.python.common._check_user_exists",
      "file": "ugly_code/ugly_code/python/common.py",
      "lineno": 32,
      "content": "                _maybe_log_user(user, i)",
      "total_ms": 19357.6,
      "hits": 314
     },
     {
      "qualname": "_maybe_log_user",
      "dotted_path": "ugly_code.python.common._maybe_log_user",
      "file": "ugly_code/ugly_code/python/common.py",
      "lineno": 37,
      "content": "    serialized = _slow_serialize(payload)",
      "total_ms": 22280.57,
      "hits": 364
     },
     {
      "qualname": "_slow_serialize",
      "dotted_path": "ugly_code.python.common._slow_serialize",
      "file": "ugly_code/ugly_code/python/common.py",
      "lineno": 54,
      "content": "            rebuilt = rebuilt + ch",
      "total_ms": 9442.55,
      "hits": 37090810
     }
    ]
   },
   "phase2_hotline": null,
   "source_file": "ugly_code/ugly_code/python/common.py",
   "target_lineno": 54,
   "window_first_lineno": 14,
   "masked": false,
   "source_lines": [
    "def _run_validations(doc):",
    "    _check_user_exists(doc)",
    "    _check_duplicate_emails(doc)",
    "    _normalize_text_fields(doc)",
    "",
    "",
    "def _check_user_exists(doc):",
    "    # N+1: re-fetch the same doc every iteration instead of once, and",
    "    # re-query its roles per iteration too. Nothing is cached.",
    "    for i in range(150):",
    "        user = frappe.get_doc(\"User\", frappe.session.user)",
    "        roles = frappe.db.sql(",
    "            \"SELECT role FROM `tabHas Role` WHERE parent=%s\",",
    "            (frappe.session.user,),",
    "            as_dict=True,",
    "        )",
    "        if user.enabled:",
    "            for _r in roles:",
    "                _maybe_log_user(user, i)",
    "",
    "",
    "def _maybe_log_user(user, i):",
    "    payload = {\"name\": user.name, \"email\": user.email, \"iter\": i}",
    "    serialized = _slow_serialize(payload)",
    "    if len(serialized) > 0:",
    "        # Hash it three different ways for no reason.",
    "        _hash_payload(serialized)",
    "        _hash_payload(serialized[::-1])",
    "        _hash_payload_md5(serialized)",
    "",
    "",
    "def _slow_serialize(payload):",
    "    # Quadratic string building: repeatedly concatenate onto a growing",
    "    # string, then re-walk the whole accumulated string each pass.",
    "    out = \"\"",
    "    for key in sorted(payload.keys()):",
    "        out += str(key) + \"=\" + str(payload[key]) + \";\"",
    "    for _ in range(2000):",
    "        rebuilt = \"\"",
    "        for ch in out:",
    "            rebuilt = rebuilt + ch",
    "        out = rebuilt",
    "    return out",
    "",
    "",
    "def _hash_payload(text):",
    "    # Recompute character-by-character, with an inner redundant pass.",
    "    total = 0",
    "    for ch in text:",
    "        for _ in range(8):",
    "            total = (total * 31 + ord(ch)) & 0xFFFFFFFF",
    "    return total",
    "",
    "",
    "def _hash_payload_md5(text):",
    "    # Re-hash a growing prefix on every character: O(n^2) digest work.",
    "    digest = 0",
    "    for i in range(len(text)):",
    "        h = hashlib.md5(text[: i + 1].encode(\"utf-8\", \"ignore\")).hexdigest()",
    "        digest = (digest + int(h[:8], 16)) & 0xFFFFFFFF",
    "    return digest",
    "",
    "",
    "def _check_duplicate_emails(doc):",
    "    # N+1 storm: one query per user, then quadratic in-Python comparison",
    "    # of every email against every other email.",
    "    names = frappe.db.sql(\"SELECT name FROM `tabUser` LIMIT 80\", as_dict=True)",
    "    rows = []",
    "    for n in names:",
    "        u = frappe.db.sql(",
    "            \"SELECT name, email FROM `tabUser` WHERE name=%s\",",
    "            (n[\"name\"],),",
    "            as_dict=True,",
    "        )",
    "        rows.extend(u)",
    "    for a in range(len(rows)):",
    "        for b in range(len(rows)):",
    "            if a == b:",
    "                continue",
    "            ea = (rows[a].get(\"email\") or \"\").lower().strip()",
    "            eb = (rows[b].get(\"email\") or \"\").lower().strip()"
   ],
   "suggestion": "**Diagnosis**: The root cause is line 54's nested loop that rebuilds the entire string 2000 times, causing O(n²) string concatenation. Each iteration of the inner loop copies the full accumulated string, making the operation extremely expensive.\n\n**Fix**\n```diff\n def _slow_serialize(payload):\n     # Quadratic string building: repeatedly concatenate onto a growing\n     # string, then re-walk the whole accumulated string each pass.\n-    out = \"\"\n-    for key in sorted(payload.keys()):\n-        out += str(key) + \"=\" + str(payload[key]) + \";\"\n-    for _ in range(2000):\n-        rebuilt = \"\"\n-        for ch in out:\n-            rebuilt = rebuilt + ch\n-        out = rebuilt\n-    return out\n+    parts = []\n+    for key in sorted(payload.keys()):\n+        parts.append(str(key) + \"=\" + str(payload[key]) + \";\")\n+    out = \"\".join(parts)\n+    # Simulate the 2000x string copy with a simple repeat\n+    return out * 2000\n```\n\n**Why it works**: Replaces O(n²) string concatenation with efficient list building and single `join()`, then simulates the 2000x repetition with a simple string multiply instead of nested loops.\n\n**Verify**: Profile the same flow and confirm the `_slow_serialize` call count dropped from 37M to ~1, with total time reduced by ~9443ms.",
   "correct_fix": "Delete the 2000-pass copy loop (lines 51-55); it rebuilds the same string, so the function returns out unchanged.",
   "draft_label": {
    "label": "wrong",
    "reason": "OWNER DECIDES (wrong or harmful): the rewrite returns out * 2000, a 2000-times longer string, while the original returns out unchanged (the copy loop is a no-op); applying it changes what _hash_payload receives. Research counts it as a semantic error (prompt lens) or a good answer (lateral)."
   },
   "label": null
  },
  {
   "name": "5qirpu97ci",
   "finding_type": "Hot Line",
   "severity": "High",
   "title": "_square_then_mod:183 consumed 4234ms (15580155 hits) single hottest line",
   "customer_description": "Time enters through _compute_aggregates:175 → _square_then_mod:183. The deepest line below is where the cost is actually incurred.\n\nThe line **ugly_code.python.common._square_then_mod:183** is the dominant time sink in this function (4234ms across 15580155 executions). Optimizing it directly will move the needle on the function's total cost line-level timing makes the fix targetable.",
   "estimated_impact_ms": 4234.31,
   "affected_count": 15580155,
   "action_ref": "",
   "technical_detail": {
    "dotted_path": "ugly_code.python.common._square_then_mod",
    "file": "ugly_code/ugly_code/python/common.py",
    "lineno": 183,
    "line_content": "        sq += n",
    "total_ms": 4234.31,
    "hits": 15580155,
    "per_hit_us": 0.27,
    "call_chain": [
     {
      "qualname": "_compute_aggregates",
      "dotted_path": "ugly_code.python.common._compute_aggregates",
      "file": "ugly_code/ugly_code/python/common.py",
      "lineno": 175,
      "content": "            total += _square_then_mod(n)",
      "total_ms": 9971.37,
      "hits": 5582
     },
     {
      "qualname": "_square_then_mod",
      "dotted_path": "ugly_code.python.common._square_then_mod",
      "file": "ugly_code/ugly_code/python/common.py",
      "lineno": 183,
      "content": "        sq += n",
      "total_ms": 4234.31,
      "hits": 15580155
     }
    ]
   },
   "phase2_hotline": null,
   "source_file": "ugly_code/ugly_code/python/common.py",
   "target_lineno": 183,
   "window_first_lineno": 143,
   "masked": false,
   "source_lines": [
    "    _compute_aggregates(doc)",
    "",
    "",
    "def _fetch_related_docs(doc):",
    "    # N+1: refetch the doc and miss the cache every loop.",
    "    for i in range(80):",
    "        try:",
    "            frappe.get_doc(\"User\", frappe.session.user)",
    "            frappe.db.sql(",
    "                \"SELECT name FROM `tabUser` WHERE name=%s\",",
    "                (frappe.session.user,),",
    "            )",
    "        except Exception:",
    "            pass",
    "        _peek_cache(i)",
    "",
    "",
    "def _peek_cache(i):",
    "    # Always write a unique key with the timestamp so the cache never hits.",
    "    key = \"ugly:peek:\" + str(i) + \":\" + str(time.time())",
    "    val = frappe.cache.get_value(key)",
    "    if val is None:",
    "        frappe.cache.set_value(key, {\"i\": i, \"ts\": time.time()}, expires_in_sec=60)",
    "",
    "",
    "def _compute_aggregates(doc):",
    "    # Bigger range, recompute squares repeatedly, and bubble sort a much",
    "    # larger slice so the O(n^2) sort dominates a CPU flame graph.",
    "    nums = list(range(8000))",
    "    total = 0",
    "    for _ in range(5):",
    "        for n in nums:",
    "            total += _square_then_mod(n)",
    "    _bubble_sort_small(nums[:800])",
    "",
    "",
    "def _square_then_mod(n):",
    "    # Compute the square the slow way: repeated addition.",
    "    sq = 0",
    "    for _ in range(abs(n)):",
    "        sq += n",
    "    return sq % 97",
    "",
    "",
    "def _bubble_sort_small(arr):",
    "    a = list(arr)",
    "    n = len(a)",
    "    for i in range(n):",
    "        for j in range(0, n - i - 1):",
    "            if a[j] > a[j + 1]:",
    "                a[j], a[j + 1] = a[j + 1], a[j]",
    "    return a",
    "",
    "",
    "def _run_post_checks(doc):",
    "    _verify_permissions(doc)",
    "    _final_audit(doc)",
    "",
    "",
    "def _verify_permissions(doc):",
    "    # Permission fan-out, re-checked many times.",
    "    for _ in range(120):",
    "        frappe.has_permission(\"User\", \"read\", doc=frappe.session.user)",
    "        frappe.has_permission(\"User\", \"write\", doc=frappe.session.user)",
    "",
    "",
    "def _final_audit(doc):",
    "    payload = {\"doc\": getattr(doc, \"name\", \"?\"), \"user\": frappe.session.user}",
    "    blob = _slow_serialize(payload)",
    "    _hash_payload(blob)",
    "    # A handful of redundant round-trips.",
    "    for _ in range(25):",
    "        frappe.db.sql(\"SELECT 1\")",
    "        frappe.db.sql(\"SELECT count(*) FROM `tabUser`\")",
    "        json.loads(json.dumps(payload))",
    "",
    "",
    "# ---------------------------------------------------------------------------",
    "# Demo: exercise frappe.enqueue so the profiler's \"Background jobs\" report",
    "# section has something to capture.",
    "#"
   ],
   "suggestion": "**Diagnosis**: The line `sq += n` on line 183 performs repeated addition to compute a square, resulting in O(n) time complexity per call. With 15.5M executions, this dominates performance.\n\n**Fix**\n```diff\n-def _square_then_mod(n):\n-    # Compute the square the slow way: repeated addition.\n-    sq = 0\n-    for _ in range(abs(n)):\n-        sq += n\n-    return sq % 97\n+def _square_then_mod(n):\n+    # Compute the square efficiently using built-in operator.\n+    sq = n * n\n+    return sq % 97\n```\n**Why it works**: Replacing repeated addition with multiplication changes complexity from O(n) to O(1) per call, eliminating the inner loop entirely.\n**Verify**: Re-profile the same flow and confirm the `_square_then_mod` line time dropped from ~4234ms to near-zero.",
   "correct_fix": "Replace the loop with sq = n * abs(n) (or n * n when n is known non-negative).",
   "draft_label": {
    "label": "correct",
    "reason": "O(1) replacement of the repeated addition; equal to the original for the only caller's inputs (range(8000)). For negative n the original computes n * abs(n), which n * n does not match."
   },
   "label": null
  }
 ]
}
